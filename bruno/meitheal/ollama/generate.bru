meta {
  name: Ollama Generate
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/ollama/generate
  body: json
  auth: none
}

body:json {
  {
    "agentId": "{{agentId}}",
    "model": "qwen3:8b",
    "prompt": "Write a TypeScript function that validates an email address",
    "system": "You are a senior frontend engineer. Return only code."
  }
}
